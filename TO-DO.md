# TO-DO - Tomorrow:
## To Test and Fix: 
- Params Class.
    - Clean up the params class. (1 day)

- CLI [1 week]
    - Write a new generate command. (1 day)
    - Write a new train command. (1 day)
    - Write a new predict command. (1 day)
    - Write a new fingerprint command. (1 day)
    - set them up so that it uses bash instead of python. 

- Setups:
    - Reformat the conda yaml file and pip requirements.txt 
    - Set it up in the GPU cluster. 
    - use poetry or pyproject to set up versions for different cuda installs. 

### Codes to Add: 
- Old Version Adaptation [1 week]
    - Port the old models and save them as their own classes. 
    - Write a v1 to v2 adapter. (2 days) 
    - Write a pre-trained model user (Supersede GraphParams for Pre-trained model. But they use all defaults) (1 day)






